def engine(table, sql):
    # EXAMPLE
    # table <str>: "table_ronny.csv" which points to a csv file such as the one i attached in the email
    # sql <str>: "SELECT account_number FROM table_ronny WHERE age LT 28 AND annual_income LT 80000"
    #
    # engine(table, sql)
    # returns ["1111", "3333"]
    return result